<h1><%= @user.name %>
  <%= link_to "Edit Profile", edit_user_path(@user), class: "btn btn-info" %>
  <%= link_to 'Permanently Delete Account',  { action: :destroy, id: @user.id }, method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' } %> </h1>

<%= render 'owned_events' %>

<%= render 'future_events' %>

<%= render 'past_events' %>
